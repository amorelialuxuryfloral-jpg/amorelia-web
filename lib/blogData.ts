/**
 * Slugs of blog posts that have been retired (old AI-generated drafts).
 * Ported from the SPA (src/lib/blogData.tsx) — requests for these URLs
 * 301 to /blog (see next.config.ts + the blog [slug] routes in both trees).
 */
export const retiredBlogSlugs: string[] = [
  "custom-bouquets-miami",
  "best-flowers-quinceaneras-miami",
  "glitter-vs-natural-bouquets",
  "how-to-choose-roses-quantity",
];
